import { GrammarHub } from "@/components/grammar";

export const metadata = {
  title: "Grammar — Fluensea",
  description:
    "Master grammar through clear explanations and progressive exercises.",
};

export default function GrammarPage() {
  return <GrammarHub />;
}
