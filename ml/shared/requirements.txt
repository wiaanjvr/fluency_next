# ml.shared — Python Dependencies
# --------------------------------

# Redis client (prediction cache + Celery broker/backend)
redis>=5.0.0

# Celery (background training jobs)
celery[redis]>=5.4.0

# HTTP client (Supabase SDK dependency + gateway health checks)
httpx>=0.27.0

# Supabase (prediction logging + GDPR deletion)
supabase>=2.3.0
postgrest>=0.16.0

# FastAPI / Uvicorn (gateway service)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Logging
loguru>=0.7.0

# Config
python-dotenv>=1.0.0
