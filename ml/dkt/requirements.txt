# Deep Knowledge Tracing — Python Dependencies
# -----------------------------------------------

# Core ML
torch>=2.2.0
numpy>=1.26.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Database
supabase>=2.3.0
postgrest>=0.16.0

# Training utilities
scikit-learn>=1.4.0
scipy>=1.12.0

# Logging & monitoring
loguru>=0.7.0

# Config
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Scheduling (for retrain cron)
apscheduler>=3.10.0

# HTTP client (used by supabase SDK)
httpx>=0.27.0

# Shared ML infrastructure (cache + prediction logging + Celery)
redis>=5.0.0
celery[redis]>=5.4.0
celery[redis]>=5.4.0
